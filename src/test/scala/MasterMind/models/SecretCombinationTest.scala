package MasterMind.models

import org.scalatest.FunSuite

class SecretCombinationTest extends FunSuite {

  test("No params Constructor") {
    val sut = new SecretCombination()
    assert(true)
  }
}